interface:
  display_name: "Long Document Segmented Writing"
  short_description: "Write long documents without losing state"
  default_prompt: "Use $long-document-segmented-writing to produce the requested source-grounded document, preserving recovery state when needed."

policy:
  allow_implicit_invocation: true
