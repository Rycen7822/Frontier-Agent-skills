interface:
  display_name: "Brainstorming"
  short_description: "Design carefully, then execute the best safe approach"
  default_prompt: "Use $brainstorming to inspect the context, choose the best-supported safe approach, and execute it when authorized."
policy:
  allow_implicit_invocation: true
