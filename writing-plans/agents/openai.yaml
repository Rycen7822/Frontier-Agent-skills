interface:
  display_name: "Writing Plans"
  short_description: "Plan implementation and useful durable handoffs"
  default_prompt: "Use $writing-plans to plan from settled decisions with enough detail for the task, then continue execution if requested."
policy:
  allow_implicit_invocation: true
