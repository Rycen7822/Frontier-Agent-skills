interface:
  display_name: "Writing Plans"
  short_description: "Compile explicit software plans and durable handoffs"
  default_prompt: "Use $writing-plans to compile settled decisions into one source-bound Handoff or update-in-place Program, ending with a precise implementation entry point for the execution phase."
policy:
  allow_implicit_invocation: true
