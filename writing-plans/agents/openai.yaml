interface:
  display_name: "Writing Plans"
  short_description: "Compile explicit software plans and durable handoffs"
  default_prompt: "Use $writing-plans to turn settled software decisions into an executable handoff or resumable program plan without executing the work."
policy:
  allow_implicit_invocation: false
