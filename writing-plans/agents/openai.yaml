interface:
  display_name: "Writing Plans"
  short_description: "Compile explicit software plans and durable handoffs"
  default_prompt: "Use $writing-plans to turn settled software decisions into a brief plan, executable handoff, or program plan without executing the work."
policy:
  allow_implicit_invocation: false
