interface:
  display_name: "Skill Evaluator"
  short_description: "Evaluate and compare Agent Skills with bounded evidence"
  default_prompt: "Use $skill-evaluator to evaluate this Agent Skill with selected cases, reusable task evidence and explicit execution budgets."

policy:
  allow_implicit_invocation: false
