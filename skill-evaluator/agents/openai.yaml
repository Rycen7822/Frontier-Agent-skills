interface:
  display_name: "Skill Evaluator"
  short_description: "Diagnose and evaluate Agent Skills with bounded evidence"
  default_prompt: "Use $skill-evaluator to assess this Agent Skill from selected historical trajectories or existing evaluation evidence. Identify justified improvements; run affected cases only when needed, with explicit execution budgets."

policy:
  allow_implicit_invocation: false
