interface:
  display_name: "Code Review"
  short_description: "Find actionable defects and assess review feedback"
  default_prompt: "Use $code-review for the requested task with proportionate evidence."
policy:
  allow_implicit_invocation: true
