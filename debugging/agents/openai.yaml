interface:
  display_name: "Debugging"
  short_description: "Find failure causes with decisive, scoped evidence"
  default_prompt: "Use $debugging for the requested task with proportionate evidence."
policy:
  allow_implicit_invocation: true
